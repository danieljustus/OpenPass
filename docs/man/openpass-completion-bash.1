.nh
.TH "OPENPASS" "1" "Apr 2026" "OpenPass" "OpenPass Manual"

.SH NAME
openpass-completion-bash - Generate the autocompletion script for bash


.SH SYNOPSIS
\fBopenpass completion bash\fP


.SH DESCRIPTION
Generate the autocompletion script for the bash shell.

.PP
This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

.PP
To load completions in your current shell session:

.EX
source <(openpass completion bash)
.EE

.PP
To load completions for every new session, execute once:

.SS Linux:
.EX
openpass completion bash > /etc/bash_completion.d/openpass
.EE

.SS macOS:
.EX
openpass completion bash > $(brew --prefix)/etc/bash_completion.d/openpass
.EE

.PP
You will need to start a new shell for this setup to take effect.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for bash

.PP
\fB--no-descriptions\fP[=false]
	disable completion descriptions


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--vault\fP="~/.openpass"
	path to the password vault


.SH SEE ALSO
\fBopenpass-completion(1)\fP
